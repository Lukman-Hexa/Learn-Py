# **TANTANGAN HARI KE-4**

### 1. Buat 4 Fungsi wajib

    def tambah(a, b):
        # mengembalikan hasil a + b
    
    def kurang(a, b):
        # mengembalikan hasil a - b
    
    def kali(a, b):
        # mengembalikan hasil a * b
    
    def bagi(a, b):
        # mengembalikan hasil a / b
        # BONUS: handle kalau b == 0

### 2. Program Utama harus:
* Minta user input angka pertama
* minta user input angka kedua
* minta user pilih operator
* panggil fungsi yang sesuai berdasarkan operator
* tampilkan hasil dengan format rapi

### 3. Validasi pembagian dengan nol(WAJIB)
* kalau user pilih / dan angka kedua = 0
* tampilkan: "Error: Tidak bisa dibagi nol!"
* jangan sampai progam crash

## **🎁 Bonus (Kerjakan setelah wajib selesai)**

- A Buath fungsi `tampilkan_menu()` yang mencetak pilih operator
- B tambahkan loop "Hitung lagi? (y/n)" setelah selesai
- Buat fungsi `input_angka(pesan)` yang otomatis handle kalau user huruf.
